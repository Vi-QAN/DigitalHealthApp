using System;

namespace WebData
{
    public class Program
    {
        public static void Main(string[] args)
        {
            // Your code here
            Console.WriteLine("Hello from the class library!");

            // Optionally, you can call other methods or instantiate classes from your class library project here

            // Keep in mind that a class library project is not an executable project,
            // so this code won't be executed directly by running the class library project itself.
            // Instead, this code can be utilized when the class library is referenced by another project.

            Console.ReadLine();
        }
    }
}
